import Card from "./components/Card";
function App() {
  return (
    <div className="App">
      <Card key={id} image={image} selected={false} onClick={() => {}} />
    </div>
  );
}

export default App;
